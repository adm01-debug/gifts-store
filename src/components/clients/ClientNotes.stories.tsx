import type { Meta } from '@storybook/react';
import { ClientNotes } from '@/components/clients/ClientNotes';
const meta: Meta = { title: 'Clients/ClientNotes', component: ClientNotes };
export default meta;
export const Default = {};
