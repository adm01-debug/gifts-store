import type { Meta } from '@storybook/react';
import { ClientForm } from '@/components/clients/ClientForm';
const meta: Meta = { title: 'Clients/ClientForm', component: ClientForm };
export default meta;
export const Default = {};
