import type { Meta } from '@storybook/react';
import { ClientStats } from '@/components/clients/ClientStats';
const meta: Meta = { title: 'Clients/ClientStats', component: ClientStats };
export default meta;
export const Default = {};
