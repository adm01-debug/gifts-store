import type { Meta } from '@storybook/react';
import { ClientDetails } from '@/components/clients/ClientDetails';
const meta: Meta = { title: 'Clients/ClientDetails', component: ClientDetails };
export default meta;
export const Default = {};
