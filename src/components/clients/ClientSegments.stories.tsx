import type { Meta } from '@storybook/react';
import { ClientSegments } from '@/components/clients/ClientSegments';
const meta: Meta = { title: 'Clients/ClientSegments', component: ClientSegments };
export default meta;
export const Default = {};
