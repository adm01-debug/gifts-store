import type { Meta } from '@storybook/react';
import { ProductPrice } from '@/components/products/ProductPrice';
const meta: Meta = { title: 'Products/ProductPrice', component: ProductPrice };
export default meta;
export const Default = {};
