import type { Meta } from '@storybook/react';
import { ProductList } from '@/components/products/ProductList';
const meta: Meta = { title: 'Products/ProductList', component: ProductList };
export default meta;
export const Default = {};
