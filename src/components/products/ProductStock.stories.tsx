import type { Meta } from '@storybook/react';
import { ProductStock } from '@/components/products/ProductStock';
const meta: Meta = { title: 'Products/ProductStock', component: ProductStock };
export default meta;
export const Default = {};
