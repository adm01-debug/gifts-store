import type { Meta } from '@storybook/react';
import { ProductCard } from '@/components/products/ProductCard';
const meta: Meta = { title: 'Products/ProductCard', component: ProductCard };
export default meta;
export const Default = {};
