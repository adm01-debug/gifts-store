import type { Meta } from '@storybook/react';
import { ProductFilters } from '@/components/products/ProductFilters';
const meta: Meta = { title: 'Products/ProductFilters', component: ProductFilters };
export default meta;
export const Default = {};
