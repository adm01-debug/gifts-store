import type { Meta } from '@storybook/react';
import { ProductSearch } from '@/components/products/ProductSearch';
const meta: Meta = { title: 'Products/ProductSearch', component: ProductSearch };
export default meta;
export const Default = {};
