import type { Meta } from '@storybook/react';
import { ProductGrid } from '@/components/products/ProductGrid';
const meta: Meta = { title: 'Products/ProductGrid', component: ProductGrid };
export default meta;
export const Default = {};
