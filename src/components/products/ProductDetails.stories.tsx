import type { Meta } from '@storybook/react';
import { ProductDetails } from '@/components/products/ProductDetails';
const meta: Meta = { title: 'Products/ProductDetails', component: ProductDetails };
export default meta;
export const Default = {};
