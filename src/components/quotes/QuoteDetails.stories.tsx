import type { Meta } from '@storybook/react';
import { QuoteDetails } from '@/components/quotes/QuoteDetails';
const meta: Meta = { title: 'Quotes/QuoteDetails', component: QuoteDetails };
export default meta;
export const Default = {};
