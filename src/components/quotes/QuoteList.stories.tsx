import type { Meta } from '@storybook/react';
import { QuoteList } from '@/components/quotes/QuoteList';
const meta: Meta = { title: 'Quotes/QuoteList', component: QuoteList };
export default meta;
export const Default = {};
