import type { Meta } from '@storybook/react';
import { QuoteCard } from '@/components/quotes/QuoteCard';
const meta: Meta = { title: 'Quotes/QuoteCard', component: QuoteCard };
export default meta;
export const Default = {};
