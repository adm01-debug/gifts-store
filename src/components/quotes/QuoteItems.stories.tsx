import type { Meta } from '@storybook/react';
import { QuoteItems } from '@/components/quotes/QuoteItems';
const meta: Meta = { title: 'Quotes/QuoteItems', component: QuoteItems };
export default meta;
export const Default = {};
