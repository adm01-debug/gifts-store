import type { Meta } from '@storybook/react';
import { QuoteTimeline } from '@/components/quotes/QuoteTimeline';
const meta: Meta = { title: 'Quotes/QuoteTimeline', component: QuoteTimeline };
export default meta;
export const Default = {};
